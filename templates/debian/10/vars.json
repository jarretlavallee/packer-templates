{
  "os_name": "debian",
  "os_version": "10",
  "guest_type": "debian10_64Guest",
  "iso_name": "debian-10.3.0-amd64-netinst.iso",
  "provision_commands": "curl -sSL https://raw.githubusercontent.com/vmware/cloud-init-vmware-guestinfo/master/install.sh | sh -",
  "boot_command": "<esc><wait>install <wait>auto <wait>url=http://{{ .HTTPIP }}:{{ .HTTPPort }}/preseed.cfg <wait>debian-installer=en_US <wait>locale=en_US <wait>keymap=us <wait>netcfg/get_domain=vm <wait>netcfg/get_hostname=localhost <wait><enter>"
}